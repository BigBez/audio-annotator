

## Change transport hint text

Update the hint text next to the transport controls in `src/pages/Index.tsx` from `Space · Enter` to `Space = Play/Pause · Enter = Mark Section`.

**File**: `src/pages/Index.tsx` — find the `<span>` containing `Space · Enter` and replace with the new text.

